package com.platzi.hibernateProject;

import org.hibernate.Session;
import org.hibernate.SessionFactory;
import org.hibernate.cfg.Configuration;


//import models
import com.platzi.hibernateProject.model.*;

/**
 * Hello world!
 *
 */
public class App 
{
    public static void main( String[] args )
    {
        System.out.println( "Hello World!" );
        
        
        /*Teacher teacher = new Teacher("Rafael Méndez Asencio", "Avatar");
        
        Course course = new Course("Java EE", "Java Collections", "PepsiCo");
        session.beginTransaction();
        session.save(course);
        session.getTransaction().commit();
        session.close();
        */
    }
}
